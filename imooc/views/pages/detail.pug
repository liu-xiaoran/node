extends ../layout

block content
    .container
        .row
            each item in movies
                .col-md-7
                    embed(src="#{movie.flash}", allowFullScree="true", quality="high", width="720", height="600", align="middle", type="application/x-shockwave-flash")
                    .col-md-5
                        dl.dl-horizontal
                            dt 电影名字
                            dd= movie.title
                            dt 
